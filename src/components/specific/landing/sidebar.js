import styles from './css-modules/side_bar.module.css';

function SideBar ({ canvasWidth }) {

    return (
        <div className={styles.container}>
            
        </div>
    );
};

export default SideBar;